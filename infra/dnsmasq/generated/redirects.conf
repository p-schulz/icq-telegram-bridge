# Legacy ICQ/AOL hostnames and what to do with them. VERIFY every line in M0:
# nothing here is confirmed until it shows up in the query log or a capture.
#
# Reminder: ICQ 5.1 has a Setup dialog where host and port can be typed in, so
# redirecting the login host may be unnecessary. It matters for the *other*
# hostnames (ads, updates, web panes).
#
# Syntax (dnsmasq):
#   address=/example.com/192.168.0.248   resolve to the lab server
#   address=/example.com/0.0.0.0        stub: connection refused fast (IPv4)
#   address=/example.com/::             stub for IPv6
#
# Candidates to observe (uncomment only once seen in the query log):
# address=/login.icq.com/192.168.0.248
# address=/login.oscar.aol.com/192.168.0.248
#
# Stub list: filled in from docs/findings.md after the M0 capture.

# Seen in the query log from ICQ 5.1 startup (2026-09-20). Resolve to live public servers.
# Measure a baseline startup first (docs/manual-tests.md #8), then enable one at a time.
# address=/xtraz.icq.com/0.0.0.0
# address=/xtraz.icq.com/::
# address=/cb.icq.com/0.0.0.0
# address=/cb.icq.com/::

